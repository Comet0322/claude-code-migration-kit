[project]
name = "migration-test-harness"
version = "0.1.0"
description = "Headless harness driving the migration kit end-to-end via the Claude Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.2.152",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
